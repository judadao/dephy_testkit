[pytest]
testpaths = tests/regression
timeout = 900
addopts = -ra
markers =
    quick: fast local regression checks
    integration: process/network/dependency integration checks
    full: slow or broad regression checks
